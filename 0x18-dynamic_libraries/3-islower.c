#include "main.h"
/**
 * _islower - program to check for lower case
 * @c: the character to check
 * Return: 1 if c is a lowercase and 0 f not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

