#include "main.h"
/**
 * _issupper - Check if a letter is upper
 * @x: The number to be checked
 * Return: 1 for upper letter or 0 for any else
 */

int _issupper(int n)

{
	if (n >= 65 && n <=90)
	{
	return (1);
	}
	return (0);
}
